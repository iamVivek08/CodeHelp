#include <iostream>
#include <vector> 
using namespace std;
/* vector is a dynamic array which allocates its memory acc. to the size of an element */
int main( ){
    vector <int > v;
  /*  v = {3, 5, 2,5};
    int n = v.size(); // size() operation tell how many element are present
    int c = v.capacity(); // capacity allocates the memory acc to the element
    // for loop to print the vector 
    for (int i:v){
        cout<<i;
    }
    */

//    to insert element at the last use pushback

  /*  v.push_back(1);
    v.push_back(2);
    v.push_back(4);
    v.push_back(5);
    for ( int i:v){
        cout <<"Capacity : "<<v.capacity() <<" " <<endl;
        cout <<i <<" " <<endl;
    }
    // cout <<" first element :"<<v.front( )<< " "<<"last element :"<<v.back();
    cout<<"Before pop :";
    for( int i:v){
        cout<<i<< endl;
    }
    // to release last element 
    v.pop_back();
    cout<<"After pop :";
    for ( int i:v){
        cout<<i<< endl;
    }
    // We can also initialise the vector in these way
    vector <int> vt(4 , 1); //  ( datatype , size)
    for ( int i: vt){
        cout << i<<" ";
    }
    cout <<"clear the element :"; 
    vt.clear();
    for ( int i: vt){
        cout << i<<" " <<endl;
    }
    cout<< "Empty or not " <<vt.empty();
    */
//    vector< vector<int>> vt;
//    vt.push_back(5);
//    vt.push_back(2);
//    vt.push_back(3);
    vector <che

    return 0;
}